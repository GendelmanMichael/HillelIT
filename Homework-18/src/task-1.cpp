#include <iostream>

using namespace std;

int main()
{

    cout << "I rememper that in log4net" << endl;
    cout << "package color of the output" << endl;
    cerr << "to cinsole text, depends on" << endl;
    cerr << "type pg output channel." << endl;
    clog << "Interesting is it's true for c++" << endl;
    clog << "Please type 1 if its true or 0 if not: " << endl;
    int res;
    cin >> res;
    cout << (res ? "It's great" : "It's sad") << endl;

    return 0;
}