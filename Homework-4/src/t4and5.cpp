#include <iostream>

using namespace std;
int main()

{

    int a = 1, b = 1, c;

    // the same as in previous task
    // code won't enter this if because c == 0 and 0 == false

    if (c = (a - b))

        cout << "The value of c is: " << c;

    return 0;
}
